import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const useDebounce = () => {
  return (
    <View>
      <Text>useDebounce</Text>
    </View>
  )
}

export default useDebounce

const styles = StyleSheet.create({})